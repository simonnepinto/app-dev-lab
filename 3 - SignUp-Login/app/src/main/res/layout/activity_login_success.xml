<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".LoginSuccessActivity"
    android:background="#298839">

    <TextView
        android:id="@+id/loginSuccessful"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Login Successful!"
        android:textSize="50sp"
        android:textStyle="bold"
        android:textColor="#114E03"
        android:fontFamily="sans-serif-condensed-light"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_marginTop="50dp"
        android:gravity="center_horizontal" />

    <ImageView
        android:id="@+id/successImage"
        android:layout_width="wrap_content"
        android:layout_height="500dp"
        app:layout_constraintTop_toBottomOf="@id/loginSuccessful"
        app:layout_constraintBottom_toTopOf="@id/logout"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        android:src="@drawable/login"
        android:layout_marginTop="100dp"/>

    <Button
        android:id="@+id/logout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:backgroundTint="#298839"
        android:text="LOGOUT"
        android:textColor="#234329"
        android:textSize="30sp"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toBottomOf="@id/successImage"
        app:layout_constraintLeft_toLeftOf="parent"
        android:layout_marginBottom="50dp"
        android:onClick="logout"/>

</androidx.constraintlayout.widget.ConstraintLayout>